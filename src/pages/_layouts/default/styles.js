import styled from 'styled-components';

import bg from '~/assets/bg_dragon.png';

export const Wrapper = styled.div`
  height: 100vh;
  background-image: url(${bg});
  background-size: cover;
  background-color: linear-gradient(-90deg, #06e774, #93f3ec);
`;
